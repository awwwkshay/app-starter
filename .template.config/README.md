# AppStarter Solution Template

This directory is a .NET Core template. To pack and use it locally:

1. Navigate to this directory: cd /Users/akshay/Developer/code/dotnet/AppStarter

2. Pack the template: dotnet new -i .

3. Use the template anywhere: dotnet new appstarter -n MyNewSolution

You can customize the template.json and add more parameters as needed.
